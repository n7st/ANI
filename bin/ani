#!/usr/bin/env perl

use strict;
use warnings;

use Data::Printer;
use FindBin;

use lib "$FindBin::RealBin/../lib";

use App::Netsplit::Injest;

my $poller = App::Netsplit::Injest->new();

p $poller->poll();

